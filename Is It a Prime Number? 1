#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int a,i,cnt=0;
    scanf("%d",&a);
    for(i=1;i<=a ;i++){
    if(a%i==0)
        cnt++;
        }  
    if(cnt==2)
        printf("Prime");
    else
        printf("Not Prime");
    return 0;
}
